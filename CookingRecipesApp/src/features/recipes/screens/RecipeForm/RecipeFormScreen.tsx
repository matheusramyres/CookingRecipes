import { Screen } from '@/shared/components/layout/Screen';
import { Text } from 'react-native';

export function RecipeFormScreen() {
  return (
    <Screen>
      <Text>RecipeFormScreen</Text>
    </Screen>
  );
}
